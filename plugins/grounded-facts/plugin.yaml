name: grounded-facts
version: "1.1.0"
description: Inject local Kiwix evidence and append deterministic source provenance to factual answers.
provides_hooks:
  - pre_llm_call
  - transform_llm_output
